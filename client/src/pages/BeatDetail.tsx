import { AudioPreview } from "@/components/AudioPreview";
import { PaymentRequestPanel } from "@/components/PaymentRequestPanel";
import { useSupabaseAuth } from "@/contexts/SupabaseAuthContext";
import { usePageMeta } from "@/hooks/usePageMeta";
import { loadBeatBySlug, loadLicenses, money, requestSecureDownload } from "@/lib/marketplace";
import type { Beat, BeatLicense } from "@/lib/models";
import { supabase } from "@/lib/supabase";
import { Download, Flag, Heart, Loader2, ShoppingBag, ShieldCheck } from "lucide-react";
import { useEffect, useState } from "react";
import { Link, useRoute } from "wouter";

export default function BeatDetail() {
  const [, params] = useRoute("/beats/:slug"); const { user } = useSupabaseAuth(); const [beat, setBeat] = useState<Beat | null>(null); const [licenses, setLicenses] = useState<BeatLicense[]>([]); const [loading, setLoading] = useState(true); const [selected, setSelected] = useState<string | null>(null); const [notice, setNotice] = useState<string | null>(null); const [paymentOpen, setPaymentOpen] = useState(false); const [reportOpen, setReportOpen] = useState(false); const [reportReason, setReportReason] = useState("Copyright concern"); const [reportDescription, setReportDescription] = useState("");
  usePageMeta(beat?.title || "Beat", beat?.description || "Preview and license an original beat on BeatBox.");
  useEffect(() => { if (!params?.slug) return; setLoading(true); loadBeatBySlug(params.slug).then(async current => { setBeat(current); if (current) { const options = await loadLicenses(current.id); setLicenses(options); setSelected(options[0]?.id ?? null); } }).catch(() => setBeat(null)).finally(() => setLoading(false)); }, [params?.slug]);
  const addToCart = async () => { if (!user || !beat) { setNotice("Sign in to add this beat to your cart."); return; } const { error } = await supabase.from("cart_items").upsert({ user_id: user.id, beat_id: beat.id, license_id: selected }, { onConflict: "user_id,beat_id,license_id" }); setNotice(error ? error.message : "Added to your cart."); };
  const download = async () => { if (!beat) return; setNotice("Preparing a short-lived private download link…"); try { const result = await requestSecureDownload(beat.id); window.location.assign(result.url); setNotice(`Secure link created. It expires in ${Math.round(result.expires_in / 60)} minutes.`); } catch (error) { setNotice(error instanceof Error ? error.message : "A verified entitlement is required before this master can be downloaded."); } };
  const favorite = async () => { if (!user || !beat) { setNotice("Sign in to save a favorite."); return; } const { error } = await supabase.from("favorites").upsert({ user_id: user.id, beat_id: beat.id }, { onConflict: "user_id,beat_id", ignoreDuplicates: true }); setNotice(error ? error.message : "Saved to your favorites."); };
  const report = async (event: React.FormEvent) => { event.preventDefault(); if (!user || !beat) { setNotice("Sign in to report content."); return; } const { error } = await supabase.from("reports").insert({ reporter_id: user.id, reported_beat_id: beat.id, reason: reportReason, description: reportDescription || null }); setNotice(error ? error.message : "Report submitted to BeatBox moderation."); if (!error) { setReportOpen(false); setReportDescription(""); } };
  if (loading) return <section className="status-page"><Loader2 className="spin" /><p>Loading beat details…</p></section>; if (!beat) return <section className="status-page"><h1>This beat is unavailable.</h1><Link className="button" href="/explore">Browse available beats</Link></section>;
  return <section className="beat-detail"><div className="container beat-detail__grid"><div className="beat-detail__cover">{beat.cover_url ? <img src={beat.cover_url} alt={`${beat.title} cover art`} /> : <span>BB</span>}</div><div className="beat-detail__main"><p className="eyebrow"><span /> {beat.genre || "Original beat"}</p><h1>{beat.title}</h1><Link className="producer-link" href={`/producers/${beat.seller_id}`}>by {beat.producer || "BeatBox producer"}</Link><p className="beat-detail__description">{beat.description || "The producer has not added a description for this beat yet."}</p><div className="metadata-row"><span>{beat.bpm || "—"} BPM</span><span>{beat.musical_key || "Key pending"}</span><span>{beat.mood || "Original mood"}</span></div><AudioPreview src={beat.preview_signed_url} title={beat.title} /><div className="beat-detail__engagement"><span><Heart size={15} /> {beat.favorite_count || 0} favorites</span><span>{beat.play_count || 0} previews</span></div><div className="beat-detail__actions"><button className="text-button" onClick={() => void favorite()}><Heart size={16} /> Save favorite</button><button className="text-button" onClick={() => setReportOpen(value => !value)}><Flag size={16} /> Report</button></div>{reportOpen && <form className="report-form" onSubmit={report}><label>Reason<select value={reportReason} onChange={event => setReportReason(event.target.value)}><option>Copyright concern</option><option>Misleading listing</option><option>Inappropriate content</option><option>Other</option></select></label><label>Details (optional)<textarea rows={2} value={reportDescription} onChange={event => setReportDescription(event.target.value)} /></label><button className="button button--small">Submit report</button></form>}</div><aside className="license-card"><div className="license-card__heading"><div><p className="eyebrow"><span /> License options</p><h2>Choose your use</h2></div><strong>{beat.is_free ? "Free" : money(beat.price)}</strong></div>{licenses.length ? <div className="license-options">{licenses.map(option => <label className={selected === option.id ? "license-option is-selected" : "license-option"} key={option.id}><input type="radio" name="license" checked={selected === option.id} onChange={() => setSelected(option.id)} /><span><b>{option.name}</b><small>{option.terms || "License details available at checkout."}</small></span><strong>{money(option.price)}</strong></label>)}</div> : <p className="license-note">The producer’s available license terms will appear here.</p>}{beat.is_free ? <button className="button license-card__button" type="button" onClick={() => void download()}><Download size={17} /> Get secure free download</button> : <><button className="button license-card__button" type="button" onClick={() => void addToCart()}><ShoppingBag size={17} /> Add to cart</button><button className="button button--outline license-card__button" type="button" onClick={() => setPaymentOpen(value => !value)}>Buy now / request payment</button></>}{notice && <p className="form-success"><ShieldCheck size={16} />{notice}</p>}{paymentOpen && !beat.is_free && <PaymentRequestPanel beat={beat} />}</aside></div></section>;
}
